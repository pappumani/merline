#include <stdio.h>
#include <string.h>
void main()
{
char t[200];
int sum= 0, i;
printf("enter the string:");
scanf("%s",&t);
for (i = 0;t[i] != '\0';i++)
{
if (a[i] == ' ')
sum++;    
}
printf("number of words in given string are: %d\n", sum+1);
}
