#include <stdio.h>
#include <string.h>
void main()
{
char a[10];
int sum =0, i;
printf("enter the string");
scanf("%[^\n]a",a);
for (i = 0;a[i] != '\0';i++)
{
if (s[i] ==' ')
sum++;    
}
printf("number of words are: %d\n", sum+1);
}
